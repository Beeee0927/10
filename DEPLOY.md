# 翼灵工作室网站 · 香港 OSS 免备案部署指南

> 目标：把 `index.html` 这套纯静态网站，用你已注册的阿里云域名 **yiling10.xyz** 访问。
> 方案：**阿里云 OSS（中国香港 Bucket）免备案托管 + 阿里云云解析（DNS 留在阿里云）**。
> 成本：按量计费，实际每月约 **1~3 元**。
> 内容：注册阿里云 → 建香港桶 → 开静态托管 → 传文件 → 绑域名 → 加解析 → 选配 HTTPS。

---

## 方案总览

| 项 | 值 |
|----|----|
| 托管 | 阿里云 OSS，中国香港地域 Bucket（免 ICP 备案） |
| 域名 | yiling10.xyz（阿里云注册，DNS 用阿里云云解析） |
| 访问方式 | 自定义域名，CNAME 指向 OSS 香港默认域名 |
| HTTPS | 可选：阿里云免费 DV 证书（3 个月） / 付费 1 年期 / 先用 HTTP |
| 月成本 | 存储约 0 元 + 流量约 0.5~5 元 ≈ **1~3 元** |

**需要的账号准备**
- 阿里云账号并完成**实名认证**（办备案/申请证书都要实名）
- 域名 yiling10.xyz 已在阿里云账号下（默认就在）

---

## 第 1 步：开通 OSS 并创建香港 Bucket

1. 登录 [阿里云控制台](https://home.console.aliyun.com/)
2. 顶部搜索框输入「**对象存储 OSS**」，进入 OSS 控制台
3. 首次使用点「**立即开通**」，勾选协议并同意（免费开通，仅按量计费）
4. 点「**创建 Bucket**」，按下表填：
   | 配置项 | 填什么 | 说明 |
   |--------|--------|------|
   | Bucket 名称 | `yiling-studio-hk` 或你自己起一个 | **全局唯一、小写**、字母数字连字符 |
   | 地域 | **中国（香港）** | 关键：选香港 → 免备案 |
   | 存储类型 | 标准存储 | 默认即可 |
   | 读写权限 | **公共读 (Public Read)** ← 必选 | 否则网站公开访问会 403 |
   | 其他 | 保持默认 | |
5. 点「创建」。

> 起 Bucket 名时如果提示已占用，换一个即可，但**名称要记住**，后面 CNAME 记录值要填它。

---

## 第 2 步：开启静态网站托管

1. 进入刚建的 Bucket
2. 左侧菜单 →「**数据管理**」（或「基础设置」）→「**静态页面**」（或「静态网站托管」）
3. 开启「静态网站托管」
   - 默认首页：`index.html`
   - 默认 404 页面：`index.html`（可选，填了就不会白屏）
4. 保存。

> ⚠️ **关键**：必须开启静态托管，且用**自定义域名**访问。
> 直接用 OSS 默认域名访问 HTML 会被阿里云强制下载（`Content-Disposition`），不渲染网页。
> 开启静态托管 + 绑定自定义域名后，才能正常在浏览器里显示。

---

## 第 3 步：上传网站文件

### 方式 A：控制台网页拖拽（最简单）
1. 进入 Bucket →「**文件管理**」→「**上传文件**」
2. 把本地这几个东西拖进去（目录会自动建）：
   - `index.html`
   - `styles.css`
   - `script.js`
   - `images/`（整个文件夹）
   - `studio-bold-showcase/`（整个文件夹）
3. 上传完成。**文件必须放在 Bucket 根目录**，不要包在多余文件夹里，否则访问路径对不上。

### 方式 B：命令行 `ossutil` 一次性同步（最稳，适合多次更新）
```bash
# 1) 配置（首次运行）
ossutil64 config
# 按提示输入：Endpoint 填 oss-cn-hongkong.aliyuncs.com；AccessKey ID/Secret（见第 4 步）

# 2) 上传整个项目到 Bucket 根目录
ossutil64 cp -r "F:\work\trae-work\6a9c12254f8e2a832abf58f5\index.html" oss://yiling-studio-hk/
ossutil64 cp -r "F:\work\trae-work\6a9c12254f8e2a832abf58f5\styles.css" oss://yiling-studio-hk/
ossutil64 cp -r "F:\work\trae-work\6a9c12254f8e2a832abf58f5\script.js"  oss://yiling-studio-hk/
ossutil64 cp -r "F:\work\trae-work\6a9c12254f8e2a832abf58f5\images"       oss://yiling-studio-hk/
ossutil64 cp -r "F:\work\trae-work\6a9c12254f8e2a832abf58f5\studio-bold-showcase" oss://yiling-studio-hk/
```

---

## 第 4 步：获取 AccessKey（仅方式 B 需要）

1. 阿里云控制台右上角头像 →「**AccessKey 管理**」
2. 建议创建一个 **RAM 子用户**，只授 OSS 权限（更安全）；测试期也可直接用主账号 AccessKey
3. 记下 **AccessKey ID** 和 **AccessKey Secret**。

> ⚠️ AccessKey 等同于账号密码，**千万不要**写进提交到公网的代码或发给他人。此处仅本机 config 使用。

---

## 第 5 步：在 OSS 绑定自定义域名

1. OSS 控制台 → 你的 Bucket →「**域名管理**」→「**绑定域名**」
2. 输入 `yiling10.xyz`，继续
3. 再点一次「绑定域名」，输入 `www.yiling10.xyz`
4. 绑定页面会给出 **CNAME 目标地址**，形如：
   ```
   yiling-studio-hk.oss-cn-hongkong.aliyuncs.com
   ```
   **把这句话记下来**，下一步要填到阿里云解析里。

---

## 第 6 步：在阿里云云解析添加 CNAME 记录

1. 阿里云控制台 → 搜索「**云解析 DNS**」→「域名解析」→ 找到 **yiling10.xyz** →「解析设置」
2. 点「**添加记录**」，加两条：
   | 记录类型 | 主机记录 | 解析线路 | 记录值 | TTL |
   |--------|---------|---------|--------|-----|
   | CNAME | `www` | 默认 | `yiling-studio-hk.oss-cn-hongkong.aliyuncs.com` | 600 |
   | CNAME | `@`（留空） | 默认 | 同上 | 600 |
   - `www` 那条：让 `www.yiling10.xyz` 访问网站
   - `@` 那条：让裸域 `yiling10.xyz` 也访问网站（OSS 对裸域同样能渲染）
3. 保存。

> 如果只想统一用 `www`、裸域 301 跳过去：阿里云云解析本身不能做裸域 301 跳转。
> 简单做法就是把 `@` 也 CNAME 到同一地址（裸域也能正常打开），不必强求域名统一。

---

## 第 7 步：验证访问

1. 等解析生效（阿里云国内解析通常几分钟内生效）
2. 浏览器打开 `http://www.yiling10.xyz`（注意先试 **http**，别加 s）
3. 出现翼灵官网页面 → **成功 ✅**
4. 若异常：
   - **404 / 空白** → 检查文件是否在 Bucket 根目录、静态托管是否开启
   - **403 / 拒绝访问** → Bucket 读写权限不是「公共读」
   - **一直转圈 / 522** → CNAME 记录值与 OSS 域名管理里显示的**完全一致**（不能带 `http://`）

---

## 第 8 步（可选）：配置 HTTPS

三种选一，按省心程度排序：

### 选择一：纯 HTTP（最省事，0 成本）
- 什么都不做，继续用第 7 步的 `http://` 地址
- 缺点：浏览器地址栏有「不安全」提示；对校园宣传站一般可接受

### 选择二：阿里云免费 DV 证书（3 个月，需每 3 个月手动换一次）
1. 控制台 →「**数字证书管理服务**」→「**SSL 证书**」→「**个人测试证书（免费）**」
2. 「立即购买」（勾协议，**0 元**支付）
3. 回页签「创建证书」，绑定域名 `yiling10.xyz`，验证方式选「**自动 DNS 验证**」（域名在阿里云会自动加验证记录，最省事）
4. 1~15 分钟签发，状态变「已签发」
5. 到 OSS →「域名管理」→ 对应域名 →「**证书管理**」→ 选择该证书 → 开启「**强制跳转 HTTPS**」
6. ⚠️ 免费证书 **3 个月后失效**，到期需重新申请 + 更新，否则 HTTPS 打不开

### 选择三：付费 DV 证书（约 68 元/年，省心）
- 1 年有效、不用频繁换，适合长期稳定运维
- 申请与部署流程同选择二，只是买的是付费版

---

## 常见坑速查（排错手册）

| 症状 | 原因 | 解决 |
|------|------|------|
| 用 OSS 默认域名访问 HTML 变成下载 | 未开静态托管 / 用了默认域名 | 开「静态网站托管」+ 用自定义域名访问 |
| 公开访问 403 | Bucket 是「私有」 | 改为「公共读」 |
| 522 / 连不上 | CNAME 上记录了 `http://` 前缀，或记录值写错 | 记录值只填纯域名，如 `xxx.oss-cn-hongkong.aliyuncs.com` |
| 页面能开但图片不显示 | 图片没传 / 传到了子目录 | 图片放 `images/` 且在 Bucket 根下 |
| 免费证书到期后打不开 | 证书只 3 个月 | 到期前重新申请并更新 |

---

## 部署完成后可顺手优化（不阻塞上线）

1. **Google Fonts 国内慢**：`index.html` 里引入了 `fonts.googleapis.com` 的 Outfit 字体，国内加载慢。可后续把字体换成国内可访问的镜像 CDN，或去掉改为系统字体。
2. **内容更新流程**：以后改页面，直接重新上传对应文件到 OSS 覆盖即可，或习惯用 `ossutil cp -r` 同步。

---

*文档基于阿里云 OSS 2026 年政策整理；控制台界面文案可能随版本微调，以「逻辑要达成的目标」为准。*
